package scalax.collection.io.json

/**
 * Import of JSON text into graphs including the steps 
 * a) parsing (extracting and checking all data from JSON texts relevant to `Graph`) and
 * b) `InputStream` creation on the basis of parsing results in preparation of calling
 * `Graph.fromStream`.
 *
 * @author Peter Empen
 */
package object imp {
}
